# TODO
- [ ] Update `Easylivin/app/signup.jsx` styles.google: fix invalid `boxShadow` and add valid shadow/elevation props.
- [ ] Improve social icon wrapper markup: change `<Text style={styles.google}>` to `<View style={styles.google}>` (and ensure the contained `<Image>` has correct sizing).
- [ ] Sanity-check for any remaining syntax errors in `signup.jsx`.
- [ ] Run the app / build to verify.

